1. [原文] 导入学生家长信息的时候，家长姓名基本为空，获取不到。所以在批量导入学生家长信息的时候，如家长姓名为空，则填入学生姓名。
[修改后的需求]之前根据学生姓名和家长姓名判断唯一性的方式也需要通过学生姓名和家长手机号来判断（同一班级，相同姓名的学生，在只有姓名和成绩的情况下，无法区分，此种情况暂不考虑，由客户随后根据商业确认）。
学生姓名	成绩	备注：家长手机号

2. [原文]导入学生信息，号码重复处理方式：首先判断第一个号码是否退订，如果未退订，状态标识标志不同，可当异网用户方式处理该信息，如（试用期为一个月，试用期到时，未交费，可直接退订该标识用户），这样的用户可不直接发送给联通进行处理，系统可自行处理。
[修改后的需求] 此需求针对联通手机用户：当导入学生家长信息时，需判断家长手机号是否在系统中存在及是否处于退订状态。如未退订，以异网用户方式处理该信息，即：试用期缩减为一周（原默认为一个月）；试用期到时，如未交费则直接退订该用户，如交费则转为现金用户（后台实现）。

小王	12345678	已存在在系统中	联通/异网

小小王	12345678	


3. [原文]接收号码，计费号码设定：考虑后期和校园智能卡绑定，进行收费，现如何实现，待客户考虑后在做处理。
[修改后的需求] 目前暂不需要做太多关注。当导入学生家长信息后，如是联通手机用户，则接收号码与计费号码相同；如是电信等异网用户，则计费号码为空。
导入文件类型为excel格式，字段包括学校全名 年级名称 班级名称 学生学号 学生姓名 家长姓名 接收手机号码 计费手机号码（配合后期校园卡推广）
特别注意：学校名称、年级、班级、学号字段不能为空, 家长信息可以为空, 学生的学号，年级，班级要在学校范围内保证唯一,每一条记录一行
导入数据库之前需提供验证页面，把错误的记录显示出来。

4. [原文]短信重发，对于已发生成功的短信可进行重发设置。方便客服对投诉未收到短信的用户进行信息重新发送处理
[修改后的需求] 需添加此功能。在短信发送查询等发送结果页面里，为每条短信发送添加checkbox，允许用户重发那些发送成功或者失败的短信。


5. [原文]系统权限设置：本次项目重点，按照(总客服-代理商-区域-客服-学校-老师)，各模块对应的功能权限设置。
[修改后的需求] 此功能需尽快实现。详述如下：
总客服即家校通管理员和家校通客服，之前设计不变；
代理商管理员分为两类：
a. 代理商管理员，此角色设计不变，可创建管理员和客服。可查看代理商负责所有学校的信息。
b. 代理商区域管理员，即代理商各区域分公司领导，浏览本区域的代理商客服和区域管理员。可创建代理商客服和同一区域的代理商区域管理员。可为代理商客服指定本区域负责的学校。可查看本区域代理商下属的所有学校的相关信息。
代理商客服设计不变。
学校信息员设计不变。
老师：创建老师时需和所在学校的班级进行关联。只能对相关班级进行短信的发送操作。






6. [原文]用户发送短信定制家校通，目前系统未做处理，需单独做一个界面，展示用户定制的信息，由代理商客服进行联系用户进行处理。
[修改后的需求] 管理平台暂不实现此界面。后台job先保存此信息。后期管理平台需实现以下功能：
a. 如果此手机号码之前曾使用过该系统的服务但目前处于退订或免费期满停止状态，则在页面显示目前此用户对应的家长和学生的相关信息，由管理员确认后修改为订购状态；
b. 如果此手机号码在系统中不存在，则显示此手机号及相关错误信息。

7. [原文]考勤：后期进行校园智能卡推广使用，学生上学时间、放学时间日期（即：日期初始设置）。
可由系统自行定义，其中,智能卡数据传输方式如何进行，待客户确认后在做决定。
[修改后的需求] 无修改。为系统扩展性考虑，可考虑在学生表中添加考勤卡号。
 
8. [原文]异网用户转现金收费实现方式：系统主界面做一个支付宝连接界面，由用户自己输入定制时间.
其中开始时间固定为当天日期，不允许用户进行修改，结束时间由用户自己选择，用户时间确定后，系统程序自动计算出收费余额（包含当前月），当用户点击确认后，连接到支付宝进行交易。
[修改后的需求] 无修改。管理平台暂不实现此功能。
 
9. [原文]家长发短信到平台，老师可进行查看信息并选择性的回复信息。
[修改后的需求] 第一阶段管理平台暂不实现此界面。后台job需实现此功能，将用户发送的短信数据插入数据库。
管理平台实现逻辑：根据家长发送短信的手机号判断家长，然后关联到学生，再由学生所在班级关联到老师。相关老师可看到负责班级学生家长的短信。

10. [原文]学生类型与入学年级匹配，可按照入学年级8月31号前来判断年级。
 [修改后的需求] 之前设计不变。后期开发将实现班级自动升级功能。
 
11. [原文]导出短信信息统计：（如统计一个代理商下最近半年（即每个月）各个学生短信发送条数统计），后期进行开发。
[修改后的需求] 此功能将在报表功能中实现。第一阶段暂不考虑。






















